import 'package:flutter/material.dart';

/// {@template shell_screen}
/// ShellScreen widget.
/// {@endtemplate}
class ShellScreen extends StatelessWidget {
  /// {@macro shell_screen}
  const ShellScreen({
    super.key, // ignore: unused_element
  });

  @override
  Widget build(BuildContext context) => const Placeholder();
}
